# NOTE: This root wasmer.toml is NOT used for deployment.
# The deploy package is the `restaurant/` folder — see restaurant/app.yaml
# (`package: .`) and restaurant/wasmer.toml, which is the manifest that
# `wasmer deploy` actually reads.
#
# Run `wasmer deploy` from the repo root; it will load restaurant/app.yaml
# and find restaurant/wasmer.toml inside the package root.
